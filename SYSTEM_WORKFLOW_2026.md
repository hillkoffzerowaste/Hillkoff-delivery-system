# Hillkoff Driver App - System Workflow 2026

เอกสารนี้ใช้เป็น workflow กลางก่อนลงมือพัฒนาเพิ่มเติม เพื่อให้ทีมเห็นภาพระบบทั้งฝ่ายขาย คนขับ และแอดมินชัดเจน ลดการแก้แบบวนไปมา และช่วยให้การเพิ่มฟีเจอร์ใหม่ไม่กระทบงานเดิม

## 1. หลักการทำงานก่อนเขียนโค้ด

```mermaid
flowchart TD
  A["รับความต้องการจากทีม"] --> B["แยกขอบเขต: ฝ่ายขาย / คนขับ / แอดมิน / รายงาน"]
  B --> C["ระบุข้อมูลที่ต้องบันทึกและสิทธิ์ที่เข้าถึงได้"]
  C --> D["วาด flow หน้าจอและสถานะงาน"]
  D --> E["เขียนโค้ดเฉพาะส่วนที่เกี่ยวข้อง"]
  E --> F["ทดสอบ: login, บันทึกข้อมูล, สถานะ, รายงาน, มือถือ"]
  F --> G["commit และ push GitHub"]
  G --> H["ติดตามปัญหาจริงจากผู้ใช้แล้ววนกลับเข้าข้อ A"]
```

กติกาหลัก: ทุกครั้งที่เพิ่มฟีเจอร์ ให้ระบุให้ได้ก่อนว่าใครใช้, เห็นข้อมูลอะไร, กดแล้วสถานะเปลี่ยนเป็นอะไร, และฝ่ายขาย/แอดมินเห็นผลอย่างไร

## 2. ภาพรวมระบบ

```mermaid
flowchart LR
  subgraph Sales["ฝ่ายขาย"]
    S1["เพิ่ม/ค้นหาลูกค้า"]
    S2["สร้างออเดอร์"]
    S3["ส่งเข้าคิวและแจ้ง LINE"]
    S4["ติดตามสถานะคนขับ"]
    S5["ดูสรุปงานวันนี้"]
  end

  subgraph Driver["คนขับ"]
    D1["ล็อกอิน"]
    D2["ตรวจสภาพรถรายวัน/รายสัปดาห์"]
    D3["รับออเดอร์ปกติ"]
    D4["รับงานวิ่งสาขา/วิ่งไกล"]
    D5["เช็คอิน ถ่ายรูป ส่ง LINE"]
    D6["ปิดงาน"]
  end

  subgraph Admin["แอดมิน"]
    A1["จัดการผู้ใช้"]
    A2["กำหนดสิทธิ์"]
    A3["ตัดสิทธิ์เท่าที่จำเป็น"]
    A4["ดูภาพรวมปัญหาและการประเมินรถ"]
  end

  subgraph Data["ฐานข้อมูลและระบบแจ้งเตือน"]
    DB["Firestore"]
    LS["Local Storage สำรองสถานะหน้าเครื่อง"]
    LINE["LINE / แชร์ข้อความแจ้งเตือน"]
    PUSH["Web Push"]
  end

  S1 --> DB
  S2 --> DB
  S3 --> DB
  S3 --> LINE
  DB --> S4
  DB --> S5
  DB --> D3
  DB --> D4
  D2 --> DB
  D5 --> DB
  D5 --> LINE
  D6 --> DB
  A1 --> DB
  A2 --> DB
  A3 --> DB
  DB --> A4
  LS --> Driver
  PUSH --> Driver
```

## 3. Workflow ออเดอร์ปกติ

```mermaid
flowchart TD
  A["ฝ่ายขายค้นหาหรือเพิ่มลูกค้า"] --> B["สร้างออเดอร์"]
  B --> C["เลือกว่าจะส่งแจ้งเตือนเข้า LINE กลุ่มคนขับหรือไม่"]
  C --> D["สถานะ: รอคนขับรับ"]
  D --> E["คนขับเห็นคิวงานในหน้าคนขับ"]
  E --> F["คนขับกดรับงาน"]
  F --> G["สถานะ: กำลังส่ง พร้อมแสดงคนขับในหน้าฝ่ายขาย"]
  G --> H["คนขับถึงปลายทาง เช็คอิน ถ่ายรูป POD"]
  H --> I["ระบบบันทึกเวลา รูป และข้อความแจ้ง LINE"]
  I --> J["สถานะ: ส่งสำเร็จ"]
  J --> K["ฝ่ายขายเห็นในสรุปส่งของวันนี้และรายงานย้อนหลัง"]
```

## 4. Workflow งานวิ่งสาขา

งานวิ่งสาขาเป็นงานที่คนขับเลือกเองจากหน้าคนขับ โดยแยกจากออเดอร์ปกติ แต่ฝ่ายขายต้องเห็นสถานะทันทีตั้งแต่เริ่มงานต้นทาง

```mermaid
flowchart TD
  A["คนขับเปิดแถบงานวิ่งสาขา"] --> B["เลือกต้นทาง"]
  B --> C["เลือกปลายทางจากสาขาที่กำหนด"]
  C --> D["กดเริ่มงานต้นทาง"]
  D --> E["ฝ่ายขายเห็นงานล่าสุด: คนขับ / ต้นทาง / ปลายทาง / เวลาเริ่ม"]
  E --> F["คนขับถึงปลายทาง"]
  F --> G["เช็คอิน ถ่ายรูป ส่ง LINE"]
  G --> H["ถ้ามีหลายจุดให้ทำทีละจุด"]
  H --> I["ปิดงาน"]
  I --> J["งานไปอยู่ในสรุปส่งของวันนี้รวมกับออเดอร์ปกติ"]
  J --> K["งานย้อนหลังเปิดดูผ่าน dropdown ในฝ่ายขาย"]
```

สาขาหลักที่ใช้ใน flow:
- สาขาช้างเผือก
- สาขาโรงงานป่าแพ่ง
- สาขาสำนักงานใหญ่
- สาขามหิดล
- สาขาทับเดื่อ

## 5. Workflow งานวิ่งไกล

งานวิ่งไกลรองรับทั้งไปสาขาเดียว และไปรอบเดียวกันสองปลายทาง เช่น หอมไกลและราติก้า

```mermaid
flowchart TD
  A["คนขับเปิดแถบงานวิ่งไกล"] --> B["เลือกทิศทาง: ขาไป หรือ ขากลับ"]
  B --> C["เลือกต้นทาง"]
  C --> D["เลือกปลายทางจุดที่ 1"]
  D --> E["เลือกปลายทางจุดที่ 2 ถ้ามี"]
  E --> F["กดเริ่มงานต้นทาง"]
  F --> G["ฝ่ายขายเห็นสถานะล่าสุดและรายละเอียดเส้นทาง"]
  G --> H["เช็คอินระหว่างทางตามความเหมาะสม"]
  H --> I["ถึงปลายทางจุดที่ 1: เช็คอิน ถ่ายรูป ส่ง LINE"]
  I --> J["ถ้ามีปลายทางจุดที่ 2 ให้ทำซ้ำ"]
  J --> K["ปิดงานขาไป"]
  K --> L["ขากลับเลือกต้นทางจากสาขาที่ไปถึง"]
  L --> M["เลือกเส้นทางกลับ เช่น ราติก้า ไป เชียงใหม่"]
  M --> N["ทำเช็คอินและปิดงานเหมือนเดิม"]
```

ปลายทางงานวิ่งไกล:
- ร้านหอมไกล จ.ชลบุรี
- สาขาราติก้า จ.กรุงเทพมหานคร
- เชียงใหม่ สำหรับงานขากลับ

## 6. Workflow ตรวจสภาพรถ

```mermaid
flowchart TD
  A["คนขับล็อกอิน"] --> B["เปิดแบบประเมินตรวจสภาพรถ"]
  B --> C["เลือกรายวันหรือรายสัปดาห์"]
  C --> D["ติกรายการตรวจครบ"]
  D --> E["เพิ่มหมายเหตุถ้ามี"]
  E --> F["กดบันทึก"]
  F --> G["ระบบบันทึกลง Firestore พร้อม driverId และวันที่"]
  G --> H["หน้า Settings แสดงภาพรวม: ใครทำแล้ว / ใครยังขาด"]
```

จุดที่ต้องคุมต่อ:
- iPhone และ Android ต้องกดบันทึกได้เหมือนกัน
- รายสัปดาห์ต้องมีปุ่มบันทึกชัดเจน
- แอดมินและฝ่ายขายควรดูภาพรวมได้ แต่คนขับเห็นเฉพาะของตัวเอง

## 7. Workflow สถานะในหน้าฝ่ายขาย

```mermaid
flowchart TD
  A["ฝ่ายขายเปิด Dashboard"] --> B["ดูคิวออเดอร์ปกติ"]
  B --> C["ดูแถบงานวิ่งสาขา / งานวิ่งไกล"]
  C --> D["แสดงงานล่าสุดพร้อมรายละเอียด"]
  D --> E["ถ้าต้องดูงานเก่า กด dropdown งานที่ผ่านมา"]
  E --> F["งานที่เสร็จแล้วถูกนับรวมในสรุปส่งของวันนี้"]
  F --> G["รายงานประจำวันเลือกย้อนหลังผ่าน dropdown"]
```

ข้อมูลที่ฝ่ายขายควรเห็นทันที:
- คนขับคนไหนเริ่มงาน
- ต้นทางและปลายทาง
- เวลาเริ่มงาน
- จุดเช็คอินล่าสุด
- รูปหลักฐานเมื่อถึงปลายทาง
- สถานะจบงาน

## 8. Workflow Login และสิทธิ์ผู้ใช้ที่จะพัฒนาต่อ

เป้าหมายคือเปลี่ยนจาก login แบบเบอร์/รหัส ไปเป็น Google login พร้อม OTP อายุ 5 นาที คล้ายระบบที่เคยทำใน ESG zerowaste

```mermaid
flowchart TD
  A["ผู้ใช้เปิดแอพ"] --> B["เลือกเข้าสู่ระบบด้วย Google"]
  B --> C{"ตรวจประเภทผู้ใช้"}
  C -->|ฝ่ายขาย| D["ต้องเป็นอีเมล @hillkoff.com เท่านั้น"]
  C -->|คนขับ| E["ใช้อีเมล Google ส่วนตัวได้"]
  C -->|แอดมิน| F["ต้องอยู่ในรายชื่อ admin allowlist"]
  D --> G["ส่ง OTP อายุ 5 นาที"]
  E --> G
  F --> G
  G --> H["ผู้ใช้กรอก OTP"]
  H --> I{"OTP ถูกต้องและยังไม่หมดอายุหรือไม่"}
  I -->|ใช่| J["สร้าง session และโหลด role/permission"]
  I -->|ไม่ใช่| K["แจ้งให้ขอ OTP ใหม่"]
  J --> L{"สิทธิ์ที่ได้รับ"}
  L -->|sales| M["เข้า Dashboard ฝ่ายขาย"]
  L -->|driver| N["เข้า Dashboard คนขับ"]
  L -->|admin| O["เข้า Settings / Admin"]
```

เงื่อนไขสำคัญ:
- ฝ่ายขาย: อนุญาตเฉพาะ Google account ที่ลงท้ายด้วย `@hillkoff.com`
- คนขับ: อนุญาต Google account ส่วนตัว แต่ต้องผูกกับ driver profile ที่แอดมินอนุมัติ
- แอดมิน: ต้องอยู่ใน allowlist และมีสิทธิ์จัดการผู้ใช้
- OTP: อายุ 5 นาที, ใช้ได้ครั้งเดียว, บันทึกเวลาขอและเวลายืนยัน
- หลัง login สำเร็จต้องบันทึก `uid`, `email`, `role`, `driverId`, `status`, `lastLoginAt`

## 9. Workflow แอดมินจัดการสิทธิ์

```mermaid
flowchart TD
  A["แอดมินเปิดหน้าจัดการผู้ใช้"] --> B["ค้นหาผู้ใช้จาก email / ชื่อ / เบอร์ / driverId"]
  B --> C["ดู role และสถานะบัญชี"]
  C --> D{"ต้องการแก้อะไร"}
  D -->|อนุมัติ| E["ตั้ง status: active"]
  D -->|พักสิทธิ์| F["ตั้ง status: suspended"]
  D -->|ตัดสิทธิ์บางส่วน| G["ปรับ permission เฉพาะที่จำเป็น"]
  D -->|เปลี่ยนบทบาท| H["แก้ role: sales / driver / admin"]
  E --> I["บันทึก audit log"]
  F --> I
  G --> I
  H --> I
  I --> J["ผู้ใช้ถูกจำกัดสิทธิ์ทันทีเมื่อเปิดแอพหรือ refresh token"]
```

สิทธิ์ที่ควรแยก:
- `orders.read`
- `orders.write`
- `orders.assign`
- `routeTasks.read`
- `routeTasks.write`
- `customers.read`
- `customers.write`
- `drivers.read`
- `drivers.write`
- `assessments.read`
- `assessments.write`
- `reports.read`
- `settings.read`
- `users.manage`
- `permissions.manage`

## 10. ตารางสิทธิ์เบื้องต้น

| ส่วนงาน | ฝ่ายขาย | คนขับ | แอดมิน |
| --- | --- | --- | --- |
| ดูลูกค้า | ได้ | เฉพาะงานที่เกี่ยวข้อง | ได้ |
| เพิ่ม/แก้ลูกค้า | ได้ | ไม่ได้ | ได้ |
| สร้างออเดอร์ | ได้ | ไม่ได้ | ได้ |
| รับงานออเดอร์ | ไม่ได้ | ได้ | ได้เฉพาะแก้ไขฉุกเฉิน |
| งานวิ่งสาขา/วิ่งไกล | ดูสถานะ | เริ่มงาน/เช็คอิน/ปิดงาน | ดูและแก้ไขได้ |
| ตรวจสภาพรถ | ดูรายงาน | ทำแบบประเมินของตัวเอง | ดูทั้งหมด |
| รายงานประจำวัน | ได้ | เฉพาะของตัวเอง | ได้ |
| ตั้งค่า/จัดการสิทธิ์ | ไม่ได้ | ไม่ได้ | ได้ |

## 11. ข้อมูลหลักที่ควรมีใน Firestore

```mermaid
flowchart LR
  U["users"] --> P["permissions"]
  U --> L["login_events"]
  U --> O["orders"]
  U --> R["route_tasks"]
  U --> A["driver_daily_assessments"]
  U --> W["driver_weekly_assessments"]
  O --> C["customers"]
  R --> C
  R --> N["notifications / LINE message log"]
```

Collection ที่ควรเตรียม:
- `users`: บัญชี, role, status, email, phone, driverId
- `permissions`: สิทธิ์รายบุคคลหรือราย role
- `otp_sessions`: OTP hash, expiresAt, usedAt, uid/email
- `login_events`: ประวัติ login และ device
- `customers`: ลูกค้า
- `orders`: ออเดอร์ปกติ
- `route_tasks`: งานวิ่งสาขาและวิ่งไกล
- `driver_daily_assessments`: ตรวจสภาพรถรายวัน
- `driver_weekly_assessments`: ตรวจสภาพรถรายสัปดาห์
- `notifications`: ประวัติ push/LINE/share message
- `audit_logs`: ประวัติแอดมินแก้ role/permission/status

## 12. Roadmap การพัฒนาต่อ

### Phase 1: ทำระบบบัญชีให้ชัด
- เพิ่ม Google login
- เพิ่ม OTP 5 นาที
- สร้าง `users`, `otp_sessions`, `audit_logs`
- บังคับฝ่ายขายใช้ `@hillkoff.com`
- ให้คนขับผูก Google ส่วนตัวกับ driver profile

### Phase 2: ทำสิทธิ์จริง
- เพิ่ม permission matrix
- จำกัดหน้าและ API ตาม role
- เพิ่มปุ่มแอดมินสำหรับ active/suspended
- บันทึก audit log ทุกครั้งที่แก้สิทธิ์

### Phase 3: ทำรายงานให้เป็นระบบ
- รวมออเดอร์ปกติ งานวิ่งสาขา งานวิ่งไกล ใน summary เดียว
- แยกดูย้อนหลังผ่าน dropdown
- เพิ่มรายงานตรวจสภาพรถรายวัน/รายสัปดาห์
- เพิ่มรายงานปัญหาและความต่อเนื่องของแอพ

### Phase 4: แข็งแรงสำหรับใช้งานจริง
- ตรวจ Firestore rules ให้ตรงกับ role/permission
- ทดสอบ iPhone/Android ก่อนปล่อยจริง
- ทดสอบ offline/online sync
- ทดสอบ LINE share และ push notification
- เพิ่ม backup และ recovery checklist

## 13. Checklist ก่อนเริ่มฟีเจอร์ใหม่ทุกครั้ง

- ระบุผู้ใช้หลักของฟีเจอร์
- ระบุหน้าจอที่เกี่ยวข้อง
- ระบุ collection/document ที่ต้องอ่านเขียน
- ระบุสถานะก่อนกดและหลังกด
- ระบุว่าฝ่ายขายเห็นอะไร
- ระบุว่าคนขับเห็นอะไร
- ระบุว่าแอดมินควบคุมสิทธิ์อะไรได้
- ระบุผลกระทบต่อรายงาน
- ทดสอบมือถืออย่างน้อย iPhone และ Android
- commit/push หลังทดสอบผ่าน
